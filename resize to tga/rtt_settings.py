from pathlib import Path

flag_folder = Path(r'.../gfx/flags') # Change to Flag folder (use '/' instead of '\')


output_size = {
    'small' : (10,7),
    'medium' : (41,26),
    'large' : (82,52)
}
