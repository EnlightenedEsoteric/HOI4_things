from pathlib import Path

flag_folder = Path(r'.../gfx/flags') # Change this to where you want the created files to be placed (use '/' instead of '\')

output_sizes = ((10,7),(41,26),(82,52)) # Change this to the dimensions of the TGA files generated, one file will be created for each pair of dimensions
# Small - 10x7
# Medium - 41x26
# Large - 82x52
